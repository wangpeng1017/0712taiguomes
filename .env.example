DATABASE_URL="file:./dev.db"
# 部署阿里云测试环境时：
# 1. 把 prisma/schema.prisma 的 datasource.provider 由 sqlite 改为 mysql
# 2. 把下面这行换成 mysql://user:password@host:3306/thailand_mes
# 3. 执行 npx prisma migrate deploy 并 npx tsx prisma/seed.ts
